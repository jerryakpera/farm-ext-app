"""
App config for the `advisory` app.
"""

# django_packages
from django.apps import AppConfig


class AdvisoryConfig(AppConfig):
    """
    Configuration for the advisory app.
    """

    name = "core.advisory"
